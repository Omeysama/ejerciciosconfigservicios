eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true         # 💡 Usar IP en lugar de nombre de host
    #hostname: localhost             # Opcional, pero ayuda en local
    instance-id: ${spring.application.name}:${server.port}
server:
  port: 7000 
